{
  "$comment": "Options every TypeScript app here agreed on before the monorepo. Anything an app genuinely needs differently (target, lib, strict, jsx) stays in the app.",
  "compilerOptions": {
    "module": "esnext",
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "noEmit": true,
    "skipLibCheck": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true
  }
}
